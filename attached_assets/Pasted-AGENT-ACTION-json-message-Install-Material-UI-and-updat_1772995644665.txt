AGENT_ACTION:
json
{
  "message": "Install Material UI and update App.tsx with improved UI and UX using Material UI components, also update the title in index.html. This will provide a better looking and more user-friendly application.",
  "planSteps": [
    "Install Material UI in the client project.",
    "Update client/src/App.tsx to use Material UI components for a better UI/UX.",
    "Update the title in client/index.html to reflect the new name (HMS).",
    "Run the application to verify the changes."
  ],
  "fileUpdates": [
    {
      "path": "client/src/App.tsx",
      "content": "import React, { useState, useEffect } from 'react';\nimport axios from 'axios';\nimport { BrowserRouter, Routes, Route, Link } from 'react-router-dom';\nimport { Container, Grid, Paper, Typography, Table, TableBody, TableCell, TableContainer, TableHead, TableRow, TextField, Button, Select, MenuItem, FormControl, InputLabel, Snackbar, Alert } from '@mui/material';\nimport { styled } from '@mui/material/styles';\n\ninterface Room {\n  id: number;\n  name: string;\n  type: string;\n  price: number;\n  availability: boolean;\n}\n\ninterface Booking {\n  id: number;\n  roomId: number;\n  guestId: number;\n  checkInDate: string;\n  checkOutDate: string;\n  numberOfGuests: number;\n  totalPrice: number;\n}\n\ninterface Guest {\n  id: number;\n  name: string;\n  email: string;\n  phone: string;\n  idDocument: string;\n}\n\nconst Item = styled(Paper)(({ theme }) => ({\n  backgroundColor: theme.palette.mode === 'dark' ? '#1A2027' : '#fff',\n  ...theme.typography.body2,\n  padding: theme.spacing(1),\n  textAlign: 'center',\n  color: theme.palette.text.secondary,\n}));\n\nconst Dashboard = () => {\n  // Sample data\n  const totalRooms = 20;\n  const occupiedRooms = 8;\n  const availableRooms = totalRooms - occupiedRooms;\n  const todaysCheckIns = 3;\n\n  return (\n    <Container>\n      <Typography variant=\"h4\" component=\"h2\" gutterBottom>\n        Dashboard\n      </Typography>\n      <Grid container spacing={3}>\n        <Grid item xs={12} sm={6} md={3}>\n          <Item>\n            <Typography variant=\"h6\">Total Rooms</Typography>\n            <Typography variant=\"h4\">{totalRooms}</Typography>\n          </Item>\n        </Grid>\n        <Grid item xs={12} sm={6} md={3}>\n          <Item>\n            <Typography variant=\"h6\">Occupied Rooms</Typography>\n            <Typography variant=\"h4\">{occupiedRooms}</Typography>\n          </Item>\n        </Grid>\n        <Grid item xs={12} sm={6} md={3}>\n          <Item>\n            <Typography variant=\"h6\">Available Rooms</Typography>\n            <Typography variant=\"h4\">{availableRooms}</Typography>\n          </Item>\n        </Grid>\n        <Grid item xs={12} sm={6} md={3}>\n          <Item>\n            <Typography variant=\"h6\">Today's Check-ins</Typography>\n            <Typography variant=\"h4\">{todaysCheckIns}</Typography>\n          </Item>\n        </Grid>\n      </Grid>\n    </Container>\n  );\n};\n\nconst Rooms = () => {\n  const [rooms, setRooms] = useState<Room[]>([]);\n  const [newRoom, setNewRoom] = useState<Room>({ id: 0, name: '', type: '', price: 0, availability: true });\n  const [open, setOpen] = useState(false);\n  const [severity, setSeverity] = useState<'success' | 'error'>('success');\n  const [message, setMessage] = useState('');\n\n  useEffect(() => {\n    fetchRooms();\n  }, []);\n\n  const fetchRooms = () => {\n    axios.get('/api/rooms')\n      .then(res => setRooms(res.data))\n      .catch(err => {\n        console.log(err);\n        handleOpen('error', 'Failed to fetch rooms');\n      });\n  };\n\n  const handleInputChange = (e: React.ChangeEvent<HTMLInputElement | HTMLSelectElement>) => {\n    setNewRoom({ ...newRoom, [e.target.name]: e.target.value });\n  };\n\n  const handleSubmit = (e: React.FormEvent) => {\n    e.preventDefault();\n    axios.post('/api/rooms', newRoom)\n      .then(res => {\n        setRooms([...rooms, res.data]);\n        setNewRoom({ id: 0, name: '', type: '', price: 0, availability: true }); // Reset the form\n        handleOpen('success', 'Room added successfully');\n        fetchRooms();\n      })\n      .catch(err => {\n        console.log(err);\n        handleOpen('error', 'Failed to add room');\n      });\n  };\n\n  const handleOpen = (severity: 'success' | 'error', message: string) => {\n    setSeverity(severity);\n    setMessage(message);\n    setOpen(true);\n  };\n\n  const handleClose = (event?: React.SyntheticEvent | Event, reason?: string) => {\n    if (reason === 'clickaway') {\n      return;\n    }\n    setOpen(false);\n  };\n\n  return (\n    <Container>\n      <Typography variant=\"h4\" component=\"h2\" gutterBottom>\n        Rooms\n      </Typography>\n      <TableContainer component={Paper}>\n        <Table sx={{ minWidth: 650 }} aria-label=\"simple table\">\n          <TableHead>\n            <TableRow>\n              <TableCell>Name</TableCell>\n              <TableCell align=\"right\">Type</TableCell>\n              <TableCell align=\"right\">Price</TableCell>\n              <TableCell align=\"right\">Availability</TableCell>\n            </TableRow>\n          </TableHead>\n          <TableBody>\n            {rooms.map((room) => (\n              <TableRow\n                key={room.id}\n                sx={{ '&:last-child td, &:last-child th': { border: 0 } }}\n              >\n                <TableCell component=\"th\" scope=\"row\">\n                  {room.name}\n                </TableCell>\n                <TableCell align=\"right\">{room.type}</TableCell>\n                <TableCell align=\"right\">{room.price}</TableCell>\n                <TableCell align=\"right\">{room.availability ? 'Yes' : 'No'}</TableCell>\n              </TableRow>\n            ))}\n          </TableBody>\n        </Table>\n      </TableContainer>\n\n      <Typography variant=\"h6\" component=\"h3\" mt={3} gutterBottom>\n        Add New Room\n      </Typography>\n      <form onSubmit={handleSubmit}>\n        <Grid container spacing={2}>\n          <Grid item xs={12} sm={6}>\n            <TextField\n              fullWidth\n              label=\"Name\"\n              name=\"name\"\n              value={newRoom.name}\n              onChange={handleInputChange}\n              required\n            />\n          </Grid>\n          <Grid item xs={12} sm={6}>\n            <FormControl fullWidth>\n              <InputLabel id=\"type-label\">Type</InputLabel>\n              <Select\n                labelId=\"type-label\"\n                name=\"type\"\n                value={newRoom.type}\n                label=\"Type\"\n                onChange={handleInputChange}\n                required\n              >\n                <MenuItem value=\"Single\">Single</MenuItem>\n                <MenuItem value=\"Double\">Double</MenuItem>\n                <MenuItem value=\"Suite\">Suite</MenuItem>\n              </Select>\n            </FormControl>\n          </Grid>\n          <Grid item xs={12} sm={6}>\n            <TextField\n              fullWidth\n              label=\"Price\"\n              name=\"price\"\
              type=\"number\"\n              value={newRoom.price}\n              onChange={handleInputChange}\n              required\n            />\n          </Grid>\n        </Grid>\n        <Button type=\"submit\" variant=\"contained\" color=\"primary\" mt={2}>\n          Add Room\n        </Button>\n      </form>\n      <Snackbar open={open} autoHideDuration={6000} onClose={handleClose}>\n        <Alert onClose={handleClose} severity={severity} sx={{ width: '100%' }}>\n          {message}\n        </Alert>\n      </Snackbar>\n    </Container>\n  );\n};\n\nconst Bookings = () => {\n  const [bookings, setBookings] = useState<Booking[]>([]);\n\n  useEffect(() => {\n    axios.get('/api/bookings')\n      .then(res => setBookings(res.data))\n      .catch(err => console.log(err));\n  }, []);\n\n  return (\n    <Container>\n      <Typography variant=\"h4\" component=\"h2\" gutterBottom>\n        Bookings\n      </Typography>\n      {/* Implement Bookings table and form here */}\n    </Container>\n  );\n};\n\nfunction App() {\n  return (\n    <BrowserRouter>\n      <Container>\n        <Typography variant=\"h3\" component=\"h1\" gutterBottom>\n          HMS - Hotel Management System\n        </Typography>\n        <nav>\n          <ul>\n            <li>\n              <Link to=\"/\">Dashboard</Link>\n            </li>\n            <li>\n              <Link to=\"/rooms\">Rooms</Link>\n            </li>\n            <li>\n              <Link to=\"/bookings\">Bookings</Link>\n            </li>\n          </ul>\n        </nav>\n        <Routes>\n          <Route path=\"/\" element={<Dashboard />} />\n          <Route path=\"/rooms\" element={<Rooms />} />\n          <Route path=\"/bookings\" element={<Bookings />} />\n        </Routes>\n      </Container>\n    </BrowserRouter>\n  );\n}\n\nexport default App;\n"
    },
    {
      "path": "client/index.html",
      "content": "<!DOCTYPE html>\n<html lang=\"en\">\n  <head>\n    <meta charset=\"UTF-8\" />\n    <link rel=\"icon\" type=\"image/svg+xml\" href="/vite.svg" />\n    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\" />\n    <title>HMS - Hotel Management System</title>\n    <link rel=\"stylesheet\" href=\"https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap\" />\n    <link rel=\"stylesheet\" href=\"https://fonts.googleapis.com/icon?family=Material+Icons\" />\n  </head>\n  <body>\n    <div id=\"root\"></div>\n    <script type=\"module\" src=\"/src/main.tsx\"></script>\n  </body>\n</html>\n"
    }
  ],
  "shellCommands": [
    "cd client && npm install @mui/material @mui/icons-material @emotion/react @emotion/styled"
  ],
  "done": false
}